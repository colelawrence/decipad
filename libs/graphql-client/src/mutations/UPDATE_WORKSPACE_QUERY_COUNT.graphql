mutation IncrementResourceUsage(
  $resourceType: ResourceTypes!
  $workspaceId: String!
  $amount: Int!
) {
  incrementResourceUsage(
    resourceType: $resourceType
    workspaceId: $workspaceId
    amount: $amount
  ) {
    id
    resourceType
    consumption
  }
}
