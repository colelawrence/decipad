mutation CreateNotebook($workspaceId: ID!, $name: String!, $sectionId: ID) {
  createPad(
    workspaceId: $workspaceId
    pad: { name: $name }
    sectionId: $sectionId
  ) {
    ...EditorNotebook
    ...WorkspaceNotebook
  }
}
