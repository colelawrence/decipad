mutation DuplicateNotebook($id: ID!, $targetWorkspace: ID!, $document: String) {
  duplicatePad(
    id: $id
    targetWorkspace: $targetWorkspace
    document: $document
  ) {
    ...EditorNotebook
    ...WorkspaceNotebook
  }
}
