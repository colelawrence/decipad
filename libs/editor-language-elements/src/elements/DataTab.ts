import { ELEMENT_DATA_TAB } from '@decipad/editor-types';
import type { InteractiveLanguageElement } from '../types';

export const DataTab: InteractiveLanguageElement = {
  type: ELEMENT_DATA_TAB,
  isStructural: true,
};
