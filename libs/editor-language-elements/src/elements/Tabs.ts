import { ELEMENT_TAB } from '@decipad/editor-types';
import { InteractiveLanguageElement } from '../types';

export const Tab: InteractiveLanguageElement = {
  type: ELEMENT_TAB,
  isStructural: true,
};
