import React from 'react';
import { CurrentWorkspace } from './CurrentWorkspace.component';

export default {
  title: 'Dashboard/Side Menu/Current Workspace',
};

export const C = () => <CurrentWorkspace />;

C.storyName = 'Current Workspace';
