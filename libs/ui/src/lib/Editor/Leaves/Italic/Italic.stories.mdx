import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { Italic } from './Italic.component';

<Meta
  title='Editor/Leaves/Italic'
  parameters={{
    docs: {
      source: {
        code: `<Italic
  attributes={{ 'data-slate-leaf': true }}
  leaf={{ text: 'Italic text' }}
  text={{ text: 'Italic text' }}
>
  Italic text
</Italic>`,
      },
    },
  }}
  argTypes={{
    children: {
      defaultValue: 'Hello World',
      description: 'The text content of the component',
      type: 'string',
    },
  }}
  component={Italic}
/>

# Italic

This leaf node makes text italic. Surprise!

export const Template = (args) => <Italic {...args} />;

<Canvas>
  <Story name='Italic'>{Template.bind({})}</Story>
</Canvas>

<Props story='Italic' />

