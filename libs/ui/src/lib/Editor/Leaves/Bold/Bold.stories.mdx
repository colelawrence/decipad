import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { Bold } from './Bold.component';

<Meta
  title="Legacy/Editor/Leaves/Bold"
  parameters={{
    docs: {
      source: {
        code: `<Bold
  attributes={{ 'data-slate-leaf': true }}
  leaf={{ text: 'Bold text' }}
  text={{ text: 'Bold text' }}
>
  Bold text
</Bold>`,
      },
    },
  }}
  argTypes={{
    children: {
      defaultValue: 'Hello World',
      description: 'The text content of the component',
      type: 'string',
    },
  }}
  component={Bold}
/>

# Bold

The bold leaf makes any text well...bold.

export const Template = (args) => <Bold {...args} />;

<Canvas>
  <Story name="Bold">{Template.bind({})}</Story>
</Canvas>

<Props story="Bold" />
