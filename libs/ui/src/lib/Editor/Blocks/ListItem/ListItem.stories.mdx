import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { ListItem } from './ListItem.component';
import { UnorderedList } from '@chakra-ui/layout';

<Meta
  title="Legacy/Editor/Blocks/Lists/ListItem"
  parameters={{
    docs: {
      source: {
        code: `<ListItem
  element={{
    children: [{ text: 'Hello World' }],
  }}
  attributes={{ 'data-slate-node': 'element', ref: null }}
>
  Hello World
</ListItem>`,
      },
    },
  }}
  argTypes={{
    children: {
      defaultValue: 'Hello World',
      description: 'The text content of the component',
      type: 'string',
    },
  }}
  component={ListItem}
/>

# ListItem

The ListItem component is just a normal `li` element that has been adapted to be a slate specific block element.

export const Template = (args) => (
  <UnorderedList>
    <ListItem {...args} />
  </UnorderedList>
);

<Canvas>
  <Story name="ListItem">{Template.bind({})}</Story>
</Canvas>

<Props story="ListItem" />
