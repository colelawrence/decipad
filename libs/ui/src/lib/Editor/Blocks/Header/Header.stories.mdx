import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { Header } from './Header.component';

<Meta
  title="Legacy/Editor/Blocks/Header"
  parameters={{
    docs: {
      source: {
        code: `<Header
  element={{
    children: [],
  }}
  attributes={{ 'data-slate-node': 'element', ref: null }}
>
  Hello World
</Header>`,
      },
    },
  }}
  argTypes={{
    children: {
      defaultValue: 'Hello World',
      description: 'The text content of the component',
      type: 'string',
    },
    size: {
      defaultValue: 'h1',
      description: 'The size of the header',
      options: ['h1', 'h2', 'h3', 'h4', 'h5', 'h6'],
      type: 'select',
    },
  }}
  component={Header}
/>

# Header

The Header block component is the slate component that renders when the user wants to input a header, there are six options for the size of the header.

# Available Sizes

- h1
- h2
- h3
- h4
- h5
- h6

export const Template = (args) => <Header {...args} />;

<Canvas>
  <Story name="Header">{Template.bind({})}</Story>
</Canvas>

<Props story="Header" />
