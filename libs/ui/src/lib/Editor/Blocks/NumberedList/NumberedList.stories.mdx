import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { NumberedList } from './NumberedList.component';
import { ListItem } from '@chakra-ui/layout';

<Meta
  title="Editor/Blocks/Lists/NumberedList"
  parameters={{
    docs: {
      source: {
        code: `<NumberedList
  element={{
    children: [],
  }}
  attributes={{ 'data-slate-node': 'element', ref: null }}
>
  <ListItem>Hello World</ListItem>
</NumberedList>`,
      },
    },
  }}
  argTypes={{
    children: {
      defaultValue: 'Hello World',
      description: 'The list items of the component',
      type: 'string',
    },
  }}
  component={NumberedList}
/>

# NumberedList

The NumberedList component is just a basic chakra ui `ol` component that has been adapted to be a slate block element component.

export const Template = (args) => (
  <NumberedList {...args}>
    <ListItem>{args.children}</ListItem>
    <ListItem>{args.children}</ListItem>
    <ListItem>{args.children}</ListItem>
  </NumberedList>
);

<Canvas>
  <Story name="NumberedList">{Template.bind({})}</Story>
</Canvas>

<Props story="NumberedList" />
