import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { BulletList } from './BulletList.component';
import { ListItem } from '@chakra-ui/react';

<Meta
  title="Legacy/Editor/Blocks/Lists/BulletList"
  parameters={{
    docs: {
      source: {
        code: `<BulletList
  element={{
    children: [{ text: 'The quick brown fox jumps over the lazy dog.' }],
  }}
  attributes={{ 'data-slate-node': 'element', ref: null }}
>
  <ListItem>The quick brown fox jumps over the lazy dog.</ListItem>
</BulletList>`,
      },
    },
  }}
  argTypes={{
    children: {
      defaultValue: 'The quick brown fox jumps over the lazy dog.',
      description: 'The list items of the component',
      type: 'string',
    },
  }}
  component={BulletList}
/>

# BulletList

The BulletList component serves as one of the editor's slate blocks which is basically just a `ul` tag component that is wrapped around `li` items.

export const Template = (args) => (
  <BulletList {...args}>
    <ListItem>{args.children}</ListItem>
    <ListItem>{args.children}</ListItem>
    <ListItem>{args.children}</ListItem>
    <ListItem>{args.children}</ListItem>
    <ListItem>{args.children}</ListItem>
  </BulletList>
);

<Canvas>
  <Story name="BulletList">{Template.bind({})}</Story>
</Canvas>

<Props story="BulletList" />
