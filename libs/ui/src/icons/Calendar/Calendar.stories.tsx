import { Meta, Story } from '@storybook/react';
import { Calendar } from './Calendar';

export default {
  title: 'Icons / Calendar',
  component: Calendar,
} as Meta;

export const Normal: Story = () => <Calendar />;
