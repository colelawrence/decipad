import { Meta, Story } from '@storybook/react';
import { Virus } from './Virus';

export default {
  title: 'Icons / Virus',
  component: Virus,
} as Meta;

export const Normal: Story = () => <Virus />;
