import { Meta, Story } from '@storybook/react';
import { Battery } from './Battery';

export default {
  title: 'Icons / Battery',
  component: Battery,
} as Meta;

export const Normal: Story = () => <Battery />;
