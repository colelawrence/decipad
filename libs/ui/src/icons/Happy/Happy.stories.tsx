import { Meta, Story } from '@storybook/react';
import { Happy } from './Happy';

export default {
  title: 'Icons / Happy',
  component: Happy,
} as Meta;

export const Normal: Story = () => <Happy />;
