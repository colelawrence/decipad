import { Meta, Story } from '@storybook/react';
import { Music } from './Music';

export default {
  title: 'Icons / Music',
  component: Music,
} as Meta;

export const Normal: Story = () => <Music />;
