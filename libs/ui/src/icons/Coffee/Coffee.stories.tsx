import { Meta, Story } from '@storybook/react';
import { Coffee } from './Coffee';

export default {
  title: 'Icons / Coffee',
  component: Coffee,
} as Meta;

export const Normal: Story = () => <Coffee />;
