import { Meta, Story } from '@storybook/react';
import { Slider } from './Slider';

export default {
  title: 'Icons / Input',
  component: Slider,
} as Meta;

export const Normal: Story = () => <Slider />;
