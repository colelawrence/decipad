import { Meta, Story } from '@storybook/react';
import { Folder } from './Folder';

export default {
  title: 'Icons / Folder',
  component: Folder,
} as Meta;

export const Normal: Story = () => <Folder />;
