import { Meta, Story } from '@storybook/react';
import { Rocket } from './Rocket';

export default {
  title: 'Icons / Rocket',
  component: Rocket,
} as Meta;

export const Normal: Story = () => <Rocket />;
