import { Meta, Story } from '@storybook/react';
import { Generic } from './Generic';

export default {
  title: 'Icons / Generic',
  component: Generic,
} as Meta;

export const Normal: Story = () => <Generic />;
