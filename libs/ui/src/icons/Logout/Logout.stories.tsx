import { Meta, Story } from '@storybook/react';
import { Logout } from './Logout';

export default {
  title: 'Icons / Logout',
  component: Logout,
} as Meta;

export const Normal: Story = () => <Logout />;
