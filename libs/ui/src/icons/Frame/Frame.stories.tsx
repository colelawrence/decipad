import { Meta, Story } from '@storybook/react';
import { Frame } from './Frame';

export default {
  title: 'Icons / Frame',
  component: Frame,
} as Meta;

export const Normal: Story = () => <Frame />;
