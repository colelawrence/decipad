import { Meta, Story } from '@storybook/react';
import { Highlight } from './Highlight';

export default {
  title: 'Icons / Highlight',
  component: Highlight,
} as Meta;

export const Normal: Story = () => <Highlight />;
