import { Meta, Story } from '@storybook/react';
import { World } from './World';

export default {
  title: 'Icons / World',
  component: World,
} as Meta;

export const Normal: Story = () => <World />;
