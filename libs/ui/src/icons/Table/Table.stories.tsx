import { Meta, Story } from '@storybook/react';
import { Table } from './Table';

export default {
  title: 'Icons / Table',
  component: Table,
} as Meta;

export const Normal: Story = () => <Table />;
