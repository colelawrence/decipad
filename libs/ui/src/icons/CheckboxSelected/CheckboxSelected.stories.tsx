import { Meta, Story } from '@storybook/react';
import { CheckboxSelected } from './CheckboxSelected';

export default {
  title: 'Icons / Checkbox / Selected',
  component: CheckboxSelected,
} as Meta;

export const Normal: Story = () => <CheckboxSelected />;
