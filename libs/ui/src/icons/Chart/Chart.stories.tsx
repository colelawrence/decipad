import { Meta, Story } from '@storybook/react';
import { Chart } from './Chart';

export default {
  title: 'Icons / Chart',
  component: Chart,
} as Meta;

export const Normal: Story = () => <Chart />;
