import { Meta, Story } from '@storybook/react';
import { Quote } from './Quote';

export default {
  title: 'Icons / Quote',
  component: Quote,
} as Meta;

export const Normal: Story = () => <Quote />;
