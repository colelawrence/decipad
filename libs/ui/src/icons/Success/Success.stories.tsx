import { Meta, Story } from '@storybook/react';
import { Success } from './Success';

export default {
  title: 'Icons / Success',
  component: Success,
} as Meta;

export const Normal: Story = () => <Success />;
