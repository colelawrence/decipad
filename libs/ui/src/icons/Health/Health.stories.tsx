import { Meta, Story } from '@storybook/react';
import { Health } from './Health';

export default {
  title: 'Icons / Health',
  component: Health,
} as Meta;

export const Normal: Story = () => <Health />;
