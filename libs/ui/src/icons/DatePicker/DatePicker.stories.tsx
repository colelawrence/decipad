import { Meta, Story } from '@storybook/react';
import { DatePicker } from './DatePicker';

export default {
  title: 'Icons / DatePicker',
  component: DatePicker,
} as Meta;

export const Normal: Story = () => <DatePicker />;
