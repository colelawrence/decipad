import { Meta, Story } from '@storybook/react';
import { Close } from './Close';

export default {
  title: 'Icons / Close',
  component: Close,
} as Meta;

export const Normal: Story = () => <Close />;
