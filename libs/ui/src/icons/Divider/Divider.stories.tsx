import { Meta, Story } from '@storybook/react';
import { Divider } from './Divider';

export default {
  title: 'Icons / Divider',
  component: Divider,
} as Meta;

export const Normal: Story = () => <Divider />;
