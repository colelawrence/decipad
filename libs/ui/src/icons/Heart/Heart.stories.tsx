import { Meta, Story } from '@storybook/react';
import { Heart } from './Heart';

export default {
  title: 'Icons / Heart',
  component: Heart,
} as Meta;

export const Normal: Story = () => <Heart />;
