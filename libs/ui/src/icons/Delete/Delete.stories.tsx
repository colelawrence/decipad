import { Meta, Story } from '@storybook/react';
import { Delete } from './Delete';

export default {
  title: 'Icons / Delete',
  component: Delete,
} as Meta;

export const Normal: Story = () => <Delete />;
