import { Meta, Story } from '@storybook/react';
import { Calculations } from './Calculations';

export default {
  title: 'Icons / Calculations',
  component: Calculations,
} as Meta;

export const Normal: Story = () => <Calculations />;
