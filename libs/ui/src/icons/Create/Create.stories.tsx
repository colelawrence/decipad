import { Meta, Story } from '@storybook/react';
import { Create } from './Create';

export default {
  title: 'Icons / Create',
  component: Create,
} as Meta;

export const Normal: Story = () => <Create />;
