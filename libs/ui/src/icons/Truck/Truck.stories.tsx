import { Meta, Story } from '@storybook/react';
import { Truck } from './Truck';

export default {
  title: 'Icons / Truck',
  component: Truck,
} as Meta;

export const Normal: Story = () => <Truck />;
