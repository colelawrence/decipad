import { Meta, Story } from '@storybook/react';
import { Plane } from './Plane';

export default {
  title: 'Icons / Plane',
  component: Plane,
} as Meta;

export const Normal: Story = () => <Plane />;
