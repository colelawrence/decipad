import { Meta, Story } from '@storybook/react';
import { Crown } from './Crown';

export default {
  title: 'Icons / Crown',
  component: Crown,
} as Meta;

export const Normal: Story = () => <Crown />;
