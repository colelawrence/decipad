import { Meta, Story } from '@storybook/react';
import { Refresh } from './Refresh';

export default {
  title: 'Icons / Refresh',
  component: Refresh,
} as Meta;

export const Normal: Story = () => <Refresh />;
