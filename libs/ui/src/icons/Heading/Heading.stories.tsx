import { Meta, Story } from '@storybook/react';
import { Heading } from './Heading';

export default {
  title: 'Icons / Heading',
  component: Heading,
} as Meta;

export const Normal: Story = () => <Heading />;
