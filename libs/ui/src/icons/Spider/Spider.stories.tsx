import { Meta, Story } from '@storybook/react';
import { Spider } from './Spider';

export default {
  title: 'Icons / Spider',
  component: Spider,
} as Meta;

export const Normal: Story = () => <Spider />;
