import { Meta, Story } from '@storybook/react';
import { Check } from './Check';

export default {
  title: 'Icons / Check',
  component: Check,
} as Meta;

export const Normal: Story = () => <Check />;
