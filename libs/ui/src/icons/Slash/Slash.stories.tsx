import { Meta, Story } from '@storybook/react';
import { Slash } from './Slash';

export default {
  title: 'Icons / Slash',
  component: Slash,
} as Meta;

export const Normal: Story = () => <Slash />;
