import { Meta, Story } from '@storybook/react';
import { DataType } from './DataType';

export default {
  title: 'Icons / DataType',
  component: DataType,
} as Meta;

export const Normal: Story = () => <DataType />;
