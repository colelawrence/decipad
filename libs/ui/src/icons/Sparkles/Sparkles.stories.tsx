import { Meta, Story } from '@storybook/react';
import { Sparkles } from './Sparkles';

export default {
  title: 'Icons / Sparkles',
  component: Sparkles,
} as Meta;

export const Normal: Story = () => <Sparkles />;
