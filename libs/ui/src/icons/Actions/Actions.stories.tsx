import { Meta, Story } from '@storybook/react';
import { Actions } from './Actions';

export default {
  title: 'Icons / Actions',
  component: Actions,
} as Meta;

export const Normal: Story = () => <Actions />;
