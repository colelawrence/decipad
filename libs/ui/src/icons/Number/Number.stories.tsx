import { Meta, Story } from '@storybook/react';
import { Number } from './Number';

export default {
  title: 'Icons / Number',
  component: Number,
} as Meta;

export const Normal: Story = () => <Number />;
