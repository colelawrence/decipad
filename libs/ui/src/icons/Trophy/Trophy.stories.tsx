import { Meta, Story } from '@storybook/react';
import { Trophy } from './Trophy';

export default {
  title: 'Icons / Trophy',
  component: Trophy,
} as Meta;

export const Normal: Story = () => <Trophy />;
