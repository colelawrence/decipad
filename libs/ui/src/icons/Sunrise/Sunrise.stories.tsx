import { Meta, Story } from '@storybook/react';
import { Sunrise } from './Sunrise';

export default {
  title: 'Icons / Sunrise',
  component: Sunrise,
} as Meta;

export const Normal: Story = () => <Sunrise />;
