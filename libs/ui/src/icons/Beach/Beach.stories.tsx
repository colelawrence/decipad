import { Meta, Story } from '@storybook/react';
import { Beach } from './Beach';

export default {
  title: 'Icons / Beach',
  component: Beach,
} as Meta;

export const Normal: Story = () => <Beach />;
