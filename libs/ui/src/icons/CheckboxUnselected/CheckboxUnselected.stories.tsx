import { Meta, Story } from '@storybook/react';
import { CheckboxUnselected } from './CheckboxUnselected';

export default {
  title: 'Icons / Checkbox / Unselected',
  component: CheckboxUnselected,
} as Meta;

export const Normal: Story = () => <CheckboxUnselected />;
