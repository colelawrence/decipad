import { Meta, Story } from '@storybook/react';
import { Education } from './Education';

export default {
  title: 'Icons / Education',
  component: Education,
} as Meta;

export const Normal: Story = () => <Education />;
