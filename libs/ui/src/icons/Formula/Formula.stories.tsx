import { Meta, Story } from '@storybook/react';
import { Formula } from './Formula';

export default {
  title: 'Icons / Formula',
  component: Formula,
} as Meta;

export const Normal: Story = () => <Formula />;
