import { Meta, Story } from '@storybook/react';
import { Percentage } from './Percentage';

export default {
  title: 'Icons / Percentage',
  component: Percentage,
} as Meta;

export const Normal: Story = () => <Percentage />;
