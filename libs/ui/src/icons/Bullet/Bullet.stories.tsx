import { Meta, Story } from '@storybook/react';
import { Bullet } from './Bullet';

export default {
  title: 'Icons / Bullet',
  component: Bullet,
} as Meta;

export const Normal: Story = () => <Bullet />;
