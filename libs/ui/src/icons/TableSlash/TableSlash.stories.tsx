import { Meta, Story } from '@storybook/react';
import { TableSlash } from './TableSlash';

export default {
  title: 'Icons / TableSlash',
  component: TableSlash,
} as Meta;

export const Normal: Story = () => <TableSlash />;
