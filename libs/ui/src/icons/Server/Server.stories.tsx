import { Meta, Story } from '@storybook/react';
import { Server } from './Server';

export default {
  title: 'Icons / Server',
  component: Server,
} as Meta;

export const Normal: Story = () => <Server />;
