import { Meta, Story } from '@storybook/react';
import { Globe } from './Globe';

export default {
  title: 'Icons / Globe',
  component: Globe,
} as Meta;

export const Normal: Story = () => <Globe />;
