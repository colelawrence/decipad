import { Meta, Story } from '@storybook/react';
import { Input } from './Input';

export default {
  title: 'Icons / Input',
  component: Input,
} as Meta;

export const Normal: Story = () => <Input />;
