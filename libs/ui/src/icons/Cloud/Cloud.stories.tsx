import { Meta, Story } from '@storybook/react';
import { Cloud } from './Cloud';

export default {
  title: 'Icons / Cloud',
  component: Cloud,
} as Meta;

export const Normal: Story = () => <Cloud />;
