import { Meta, Story } from '@storybook/react';
import { Italic } from './Italic';

export default {
  title: 'Icons / Italic',
  component: Italic,
} as Meta;

export const Normal: Story = () => <Italic />;
