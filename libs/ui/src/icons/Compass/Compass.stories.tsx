import { Meta, Story } from '@storybook/react';
import { Compass } from './Compass';

export default {
  title: 'Icons / Compass',
  component: Compass,
} as Meta;

export const Normal: Story = () => <Compass />;
