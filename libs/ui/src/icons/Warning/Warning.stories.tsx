import { Meta, Story } from '@storybook/react';
import { Warning } from './Warning';

export default {
  title: 'Icons / Warning',
  component: Warning,
} as Meta;

export const Normal: Story = () => <Warning />;
