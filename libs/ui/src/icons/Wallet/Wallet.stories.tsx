import { Meta, Story } from '@storybook/react';
import { Wallet } from './Wallet';

export default {
  title: 'Icons / Wallet',
  component: Wallet,
} as Meta;

export const Normal: Story = () => <Wallet />;
