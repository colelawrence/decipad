import { Meta, Story } from '@storybook/react';
import { Medal } from './Medal';

export default {
  title: 'Icons / Medal',
  component: Medal,
} as Meta;

export const Normal: Story = () => <Medal />;
