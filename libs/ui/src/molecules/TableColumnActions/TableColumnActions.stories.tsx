import { Meta, Story } from '@storybook/react';
import { TableColumnActions } from './TableColumnActions';

export default {
  title: 'Molecules / Table / Column Actions',
  component: TableColumnActions,
} as Meta;

export const Menu: Story = () => <TableColumnActions />;
