import { enableMapSet } from 'immer';
export * from './runtime';

enableMapSet();

// Global stuff:
// Result (returned from resultAt) { type: Type (global), value: number[] }
// AST.*
// Type

/*
Functions exported
createRuntime
*/
