{
  "name": "frontend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/frontend",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./scripts/frontend.sh",
        "cwd": "apps/frontend"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node build-live-connect-worker.mjs && yarn vite build --mode production",
        "cwd": "apps/frontend"
      }
    },
    "build-live-connect-worker": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node build-live-connect-worker.mjs",
        "cwd": "apps/frontend"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node build-live-connect-worker.mjs && yarn vite build --mode development && vite preview",
        "cwd": "apps/frontend"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "options": {
        "lintFilePatterns": ["apps/frontend/**/*.{ts,tsx}"],
        "maxWarnings": 0
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn tsc -b apps/frontend"
      }
    },
    "depcheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "../../node_modules/.bin/depcruise --config ../../.dependency-cruiser.js src",
        "cwd": "apps/frontend"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/apps/frontend"],
      "options": {
        "command": "npx vitest --run",
        "cwd": "apps/frontend"
      }
    }
  },
  "tags": []
}
