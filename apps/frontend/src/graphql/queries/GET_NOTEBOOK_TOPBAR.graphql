fragment TopbarNotebook on Pad {
  id
  name
  myPermissionType
  isPublic
  access {
    users {
      user {
        id
        name
      }
      permission
    }
  }
  workspace {
    id
    name
  }
}

query GetNotebookTopbar($id: ID!) {
  getPadById(id: $id) {
    ...TopbarNotebook
  }
}
