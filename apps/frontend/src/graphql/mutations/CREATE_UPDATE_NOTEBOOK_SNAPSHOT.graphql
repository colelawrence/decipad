mutation CreateOrUpdateNotebookSnapshot(
  $notebookId: ID!
  $snapshotName: String!
) {
  createOrUpdateSnapshot(notebookId: $notebookId, snapshotName: $snapshotName) {
    id
    snapshots {
      ...NotebookSnapshot
    }
  }
}
