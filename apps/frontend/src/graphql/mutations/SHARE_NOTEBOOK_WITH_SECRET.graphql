mutation ShareNotebookWithSecret(
  $id: ID!
  $permissionType: PermissionType!
  $canComment: Boolean!
) {
  sharePadWithSecret(
    id: $id
    permissionType: $permissionType
    canComment: $canComment
  )
}
