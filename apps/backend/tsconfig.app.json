{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node", "express"]
  },
  "exclude": ["**/*.spec.*"],
  "include": ["**/*.js"],
  "references": [
    {
      "path": "../../libs/kafka-lite"
    }
  ]
}
